"""This is the program of understading the sets in python"""

# set only stores values and it automatically removes the duplicate values
# set does't print or display the values in order whcih we put the order of displayting the values will be random

s = {}

e = set()  # to create the empty set we must define it as somethigng = set() not s ={} this will be defines as empty dictionary
